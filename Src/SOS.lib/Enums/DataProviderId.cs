namespace SOS.Lib.Enums
{
    /// <summary>
    /// Id of data providers, must be binary unique
    /// </summary>
    public enum DataProvider
    {
        SpeciesPortal = 1,
        ClamPortal = 2,
        KUL = 4,
        TaxonService = 8
    }
}
