apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sosadminapi-ingress
  namespace: sosadminapi
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 20m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - sos-hangfire.artdata.slu.se
      secretName: wildcard.artdata.slu.se-tls
  rules:
  - host: sos-hangfire.artdata.slu.se
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sosadminapi-api
            port:
              number: 5005